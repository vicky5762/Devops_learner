pipeline {
    agent any

    stages {

        stage('Build') {
            steps {
                sh 'java -version'
            }
        }

        stage('Deploy') {
            steps {
                sh 'echo Deploying application'
            }
        }

    }
}